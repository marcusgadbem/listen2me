 <!-- Top Header -->
        <div class="header-wrapper bg--grey">
            <div class="row">
                <header>
                    <div class="small-x medium-3 large-3 columns">
                        <a href="/" class="logo">Listen 2 Me</a>
                    </div>

                    <div id="login--box" class="small-x medium-x large-5 columns text-right">
                        <span class="username">Hello, <%= current_user.name  %> ! <a href="/logout" >(Logout)</a></span>
                    </div>
                </header>
            </div>
        </div>
        <!-- Top Header eof -->


        <div class="row channel--index">
            <div class="small-x medium-8 large-8 columns">
                <div class="title-wrapper">
                    <h1>All Channels (<%= channels_count %>)</h1>
                </div>
                <% if (channels.length > 0) {
                    channels.forEach( function( chan ){ %>
                    <%- include _channel-item.ejs %>
                <% }); } else { %>
                    <p>No channels created yet.</p>
                <% } %>

                <%
                    if (pages > 1) {
                %>
                    Page:
                <%
                        var x=1;
                        for(x; x <= pages; ++x) {
                            if (x == page) {
                %>
                    <strong><%= x %></strong>
                <%
                            } else {
                %>
                    <a href="channels?page=<%= x %>"><%= x %></a>
                <%
                            }
                        }
                    }
                %>
            </div>

            <div class="small-x medium-4 large-4 columns">
                <div class="nav-wrapper">
                    <h1>or... Create yours!</h1>
                </div>
                <p>Create your own channel and invite with your friends.</p>
                <%- include _form-new-channel.ejs %>
            </div>

        </div> <!-- row channel--index -->

        <br><br><br>
        <!-- Footer -->
        <div class="bg--grey">
            <footer>
                <div class="row">
                    <div class="small-x medium-x large-12 columns">
                        Foterzinho
                    </div>
                </div>
            </footer>

        </div>
        <!-- Footer eof -->
